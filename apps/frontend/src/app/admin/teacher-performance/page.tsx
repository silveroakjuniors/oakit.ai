'use client';
// Admin teacher performance view — reuses same data as principal
// Fetches from /api/v1/principal/teacher-performance (accessible to admin role too)
export { default } from '@/app/principal/teacher-performance/page';
